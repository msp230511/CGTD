<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <title>CGTD</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

  </head>

  <body>
    <div class="container-fluid">
      <div class="row" id="header">
        <div class="col-md-2">
          <%= image_tag("logo-color.png", height:200, width:200, alt:"CGTD-logo") %>
        </div>
      </div>
    </div>
    <% if flash[:notice] %>
        <div class="row">
          <div class="col-md-4 alert alert-primary notice">
            <%= "#{flash[:notice]}" %>
          </div>
        </div>
      <% end %>
      <% if flash[:alert] %>
        <div class="row">
          <div class="col-md-4 alert alert-warning alert">
            <%= "#{flash[:alert]}" %>
          </div>
        </div>
      <% end %>
    <%= yield %>
  </body>
</html>

  