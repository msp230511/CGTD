<%= form_with(model: todo_entry) do |form| %>
  <% if todo_entry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(todo_entry.errors.count, "error") %> prohibited this todo_entry from being saved:</h2>

      <ul>
        <% todo_entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
